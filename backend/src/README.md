# Backend Source Layout

This folder represents the canonical source layout for backend deliverables.

- `src/` application and library source files
- `tests/` automated tests
- `infra/` migration scripts and deployment assets

Current implementation keeps runtime code in `backend/etl-function` and `backend/api`.
